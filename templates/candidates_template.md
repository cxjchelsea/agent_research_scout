# Candidate Directions: {{scout_run_name}}

> **Phase**：Discovery
> **生成日期**：
> **扫描范围**：见 `scout/landscape_scan.md`
> **用途**：从广搜中提出 3–7 个候选方向，供人工选择 1 个进入 `topics/<topic>/` 深研

**candidate_id 规则**：为每个候选分配稳定 ID（如 C1、C2…），与 `discovery_paper_table.csv` 的 `candidate_id` 列对齐。

**论据要求**：每个进入候选表的方向，至少应在 `discovery_paper_table.csv` 中有 **3 篇** verified 或 uncertain 来源支撑；**最大威胁 work 必须 verified**，否则只能作为风险线索，不能作为核心否定依据。

---

## 1. 扫描摘要

（2–4 句：本次广搜覆盖了哪些方向、总体空白感、最大威胁是什么）

---

## 2. 候选方向概览

| Rank | ID | 候选名称 | 一句话定义 | 贡献类型 | 支撑论文数 | 总分（/10） | 档位 | 最大威胁 | 推荐度 |
|------|----|----------|------------|----------|------------|-------------|------|----------|--------|
| 1 | C1 | | | A/B/C/D/E | ≥3 | | 强烈/可做/谨慎/不建议 | | ⭐⭐⭐ |
| 2 | C2 | | | | | | | | |
| 3 | C3 | | | | | | | | |

**档位对照**（十维均分）：8.5–10 强烈值得做 · 7–8.4 可以做 · 5–6.9 谨慎 · 低于 5 不建议

---

## 3. 十维评分矩阵

与 `paper_research_skill.md` §三 对齐；每项 1–10 分，**风险可控**表示审稿/撞车风险越低分越高。

| ID | 重要性 | 新颖性 | 可形式化 | 可验证性 | 方法空间 | 泛化性 | 顶会匹配 | 工程可行 | Baseline清晰 | 风险可控 | 均分 |
|----|--------|--------|----------|----------|----------|--------|----------|----------|--------------|----------|------|
| C1 | | | | | | | | | | | |
| C2 | | | | | | | | | | | |
| C3 | | | | | | | | | | | |

**排序规则**：先按均分降序；均分接近时，优先 **可验证性 + Baseline清晰 + 风险可控** 更高的候选。

---

## 4. 候选详情

### C1：{{name}}

- **问题是否真实**：
- **已有相似工作**：
- **支撑论文**（≥3 篇，来自 `discovery_paper_table.csv`）：
  - title · verified_status
  - title · verified_status
  - title · verified_status
- **最大威胁 work**：（必须 verified；若仅 uncertain，标注为风险线索）
- **可能创新点**：
- **MVP 验证思路**（benchmark / baseline / metric）：
- **最大风险**：
- **为何进入 top-k**：

### C2：{{name}}

（同上）

---

## 5. 不建议继续的方向（可选）

| 方向 | 为何排除 |
|------|----------|
| | 已被完整覆盖 / 只是换名 / 无法构造验证 / 支撑论文不足 |

---

## 6. 推荐进入深研的 1–2 个方向

| 推荐 | ID | 理由 | 建议 topic 目录名 |
|------|----|------|-------------------|
| 首选 | | | `topics/<slug>/` |
| 备选 | | | `topics/<slug>/` |

---

## 7. 人工选择（Selection）

> 用户确认后，Agent 写入 `scout/selection.md`（模板：`templates/selection_template.md`）；**不得**自行替用户做最终选择。

- **待选 ID**：（C1 / C2 / …，由用户确认）
- **下一步**：用户确认 → 填写 `scout/selection.md` → 创建 `topics/<slug>/` → Phase 3 Deep Dive
